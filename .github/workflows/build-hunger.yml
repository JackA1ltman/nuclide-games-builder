name: Build They Hunger

permissions:
  contents: write
  actions: write

on:
  workflow_call:

jobs:
  build-valve:
    strategy:
      fail-fast: true
    uses: ./.github/workflows/build-process-older.yml
    secrets: inherit
    with:
      game_repo: "fn/hunger"
      game_name: "hunger"
