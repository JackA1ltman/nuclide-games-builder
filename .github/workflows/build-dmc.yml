name: Build Deathmatch Classic

permissions:
  contents: write
  actions: write

on:
  workflow_call:

jobs:
  build-valve:
    strategy:
      fail-fast: true
    uses: ./.github/workflows/build-process.yml
    secrets: inherit
    with:
      game_repo: "fn/dmc"
      game_name: "dmc"
